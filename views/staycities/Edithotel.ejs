<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Hotel</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            width: 100%;
            max-width: 600px;
            padding: 20px;
        }
        .form-section {
            width: 100%;
        }
        .form-group {
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container mt-4 shadow-lg p-4 rounded">
        <div id="edit" class="form-section" style="margin-top: 80%;">
            <div class="btn-group mb-4" role="group" aria-label="Basic example">
                <button type="button" class="btn btn-primary" >chnage</button>
            </div>
            <h3>Edit Hotel</h3>
            <form action="/hotels/edit/<%= staycity._id %>/<%= hotel._id %>" method="POST" onsubmit="closeWindow()">
                <div class="form-group">
                    <label for="title">Title</label>
                    <input type="text" class="form-control" id="title" name="hotel[title]" placeholder="<%= hotel.title %>">
                </div>
                <div class="form-group">
                    <label for="brand">Hotel Brand</label>
                    <input type="text" class="form-control" id="brand" name="hotel[brand]" placeholder="<%= hotel.brand %>">
                </div>
                <div class="form-group">
                    <label for="area">Area</label>
                    <input type="text" class="form-control" id="area" name="hotel[area]" placeholder="<%= hotel.area %>">
                </div>
                <div class="form-group">
                    <label for="address">Address</label>
                    <input type="text" class="form-control" id="address" name="hotel[address]" placeholder="<%= hotel.address %>">
                </div>
                <div class="form-group">
                    <label for="nearby">Near By</label>
                    <input type="text" class="form-control" id="nearby" name="hotel[nearby]" placeholder="<%= hotel.nearby %>">
                </div>
                <div class="form-group">
                    <label for="capacity">Capacity</label>
                    <input type="text" class="form-control" id="capacity" name="hotel[capacity]" placeholder="<%= hotel.capacity %>">
                </div>
                <div class="form-group">
                    <label for="view">View Type</label>
                    <input type="text" class="form-control" id="view" name="hotel[view]" placeholder="<%= hotel.view %>">
                </div>
                <div class="form-group">
                    <label for="rating">Rating</label>
                    <input type="number" class="form-control" id="rating" name="hotel[rating]" min="1" max="5" placeholder="<%= hotel.rating %>">
                </div>
                <div class="form-group">
                    <label for="category">Category</label>
                    <select class="form-select" name="hotel[category]" id="category">
                        <option value="Resort" <% if (hotel.category === 'Resort') { %> selected <% } %>>Resort</option>
                        <option value="Apartment" <% if (hotel.category === 'Apartment') { %> selected <% } %>>Apartment</option>
                        <option value="House" <% if (hotel.category === 'House') { %> selected <% } %>>House</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="meals">Meals Inclusion</label>
                    <select class="form-select" name="hotel[meals]" id="meals">
                        <option value="No meals included" <% if (hotel.meals === 'No meals included') { %> selected <% } %>>No meals included</option>
                        <option value="Free breakfast" <% if (hotel.meals === 'Free breakfast') { %> selected <% } %>>Free Breakfast</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="cancellation">Cancellation Policy</label>
                    <input type="text" class="form-control" id="cancellation" name="hotel[cancellation]" placeholder="<%= hotel.cancellation %>">
                </div>
                <div class="form-group">
                    <label for="oldprice">Old Price</label>
                    <input type="number" class="form-control" id="oldprice" name="hotel[oldprice]" placeholder="<%= hotel.oldprice %>">
                </div>
                <div class="form-group">
                    <label for="newprice">New Price</label>
                    <input type="number" class="form-control" id="newprice" name="hotel[newprice]" placeholder="<%= hotel.newprice %>">
                </div>
                <button type="submit" class="btn btn-success">Submit</button>
            </form>
            
        </div>
    </div>
    <script>
        function closeWindow() {
            setTimeout(() => {
                window.opener.location.reload();
                window.close();
            }, 500); // Adjust timeout as needed
        }
    </script>
</body>
</html>
